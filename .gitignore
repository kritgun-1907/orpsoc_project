data/*.pkl
*.pkl
*.pyc
__pycache__/
.DS_Store
drift.pkl
*.png
# Checkpoints: regenerable, provenance-scoped, can get large
results/checkpoints/

# Report figures ARE deliverables (the .md sources reference them).
# The blanket *.png above is for regenerable plots/ output, not these.
!reports/**/*.png

# never track macOS metadata, even under reports/
reports/**/.DS_Store
